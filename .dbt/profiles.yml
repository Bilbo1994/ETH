eth:
  outputs:
    ci:
      account: RAJJRGF-MX53678
      database: CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: ACCOUNTADMIN
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: BILBO
      warehouse: compute_wh

  target: ci